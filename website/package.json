{
  "name": "website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sync": "node sync.js",
    "dev": "pnpm run sync && vitepress dev docs",
    "build": "pnpm run sync && vitepress build docs",
    "preview": "pnpm run sync && vitepress preview docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vitepress": "1.0.0-alpha.46",
    "vue": "^3.2.47"
  }
}
